<nav class="nav">
  <div class="left">
  <%= link_to "All Horses", horses_path, class: "button" %>
  <%= link_to "New Horse", new_horse_path, class: "button" %>
  </div>
  <div class="right">
    <% if current_user %>
      <%= link_to current_user.name, current_user %>
      <%= link_to "Sign Out", session_path, method: :delete, class: "button" %>
    <% else %>
      <%= link_to("Sign Up", signup_path) %>
      <%= link_to("Sign In", signin_path) %>
    <% end %>
  </div>
</nav>